---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-stats
spec:
  template:
    spec:
      containers:
        - name: network-stats
          env:
            - name: ENVIRONMENT
              value: staging
